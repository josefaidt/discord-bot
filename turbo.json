{
  "$schema": "https://turborepo.org/schema.json",
  "globalDependencies": [".env", "$CDK_DEFAULT_ACCOUNT", "$CDK_DEFAULT_REGION"],
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["build/**"]
    },
    "lint": {
      "outputs": []
    },
    "dev": {
      "cache": false
    }
  }
}
